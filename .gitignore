node_modules/
dist/
.env
*.log
# Cover server/data/ and any other server/data*/ smoke/scratch dirs
server/data*/
*.db
*.db-shm
*.db-wal
.mcp.json
tmp/
# Populated production secret — copy from k8s/secret.example.yaml, fill in,
# apply with kubectl. Never commit.
k8s/secret.yaml
