apiVersion: v1
kind: Service
metadata:
  name: waha-claude
  namespace: waha-claude
spec:
  type: ClusterIP
  selector:
    app: waha-claude
  ports:
    - name: http
      port: 80
      targetPort: 8080
