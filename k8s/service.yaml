apiVersion: v1
kind: Service
metadata:
  name: waha-claude-service
  namespace: waha-claude-prod
spec:
  selector:
    app: waha-claude
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
